{
  "name": "online-gencad-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node -e \"require('fs').renameSync('dist/index.html','dist/online-gencad-viewer.html')\"",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.3.0",
    "vite-plugin-singlefile": "^2.0.3"
  },
  "dependencies": {
    "@leafer-in/view": "^2.1.0",
    "@leafer-in/viewport": "^2.1.0",
    "leafer-ui": "^2.1.0"
  }
}
